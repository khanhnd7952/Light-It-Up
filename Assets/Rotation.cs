using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rotation : MonoBehaviour
{

    public static GameObject human;
    // Update is called once per frame

    public static void RotateIfNotGround()
    {
        // kiểm tra nếu không chạm đất thì rotate


        //if (!Circle.isTouchObject)
        //{
        //    human.transform.Rotate(new Vector3(0, 0, 1) * 200 * Time.deltaTime);
        //}
    }
}
